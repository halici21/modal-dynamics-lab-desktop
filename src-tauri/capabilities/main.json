{
  "identifier": "main-window",
  "description": "Window focus lifecycle only; no filesystem, shell or network access.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-is-focused"
  ]
}
